---
"@deox/dom-to-react": patch
---

feat: call `body` callback with `HTMLBodyElement` after parsing
feat: allow accessing props and children with `props` and `children` function in `transform` callback
